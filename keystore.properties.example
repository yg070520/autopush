# 本地签名配置模板 —— 复制为 keystore.properties 并填写真实值
# keystore.properties 已加入 .gitignore，不会提交到 Git

storeFile=release.jks
storePassword=your_store_password
keyAlias=your_key_alias
keyPassword=your_key_password
